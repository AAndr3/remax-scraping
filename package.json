{
  "name": "remax-scraper",
  "version": "1.0.0",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install chromium && cp -r /opt/render/.cache/ms-playwright /opt/render/project/.playwright"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.42.1"
  }
}
